<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Ultra Transport & Logistics</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
/* ================= ROOT COLORS ================= */
:root{
  --bg:#020617;
  --glass:rgba(15,23,42,.75);
  --primary:#22d3ee;
  --accent:#facc15;
  --text:#e5e7eb;
  --muted:#94a3b8;
}

/* ================= GLOBAL ================= */
*{
  margin:0;
  padding:0;
  box-sizing:border-box;
  font-family:"Poppins",sans-serif;
}
body{
  background:var(--bg);
  color:var(--text);
}

/* ================= NAVBAR ================= */
.navbar{
  position:fixed;
  top:0;
  width:100%;
  padding:16px 70px;
  display:flex;
  justify-content:space-between;
  align-items:center;
  background:linear-gradient(90deg,#020617,#020617,#020617);
  backdrop-filter:blur(16px);
  z-index:1000;
  border-bottom:1px solid rgba(255,255,255,.05);
}

.logo{
  font-size:30px;
  font-weight:800;
  background:linear-gradient(45deg,var(--primary),var(--accent));
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
}

.menu{
  display:flex;
  gap:40px;
}

.menu-item{
  position:relative;
}

.menu-item>a{
  color:var(--text);
  text-decoration:none;
  font-size:16px;
  padding:8px 0;
  position:relative;
}

/* underline animation */
.menu-item>a::after{
  content:"";
  position:absolute;
  left:0;
  bottom:-6px;
  width:0;
  height:2px;
  background:linear-gradient(90deg,var(--primary),var(--accent));
  transition:.4s;
}
.menu-item>a:hover::after{
  width:100%;
}

/* ================= DROPDOWN (ULTRA CARD STYLE) ================= */
.dropdown{
  position:absolute;
  top:52px;
  left:0;
  min-width:220px;
  background:var(--glass);
  border-radius:18px;
  padding:10px;
  box-shadow:0 25px 60px rgba(0,0,0,.6);
  opacity:0;
  visibility:hidden;
  transform:translateY(15px);
  transition:.35s;
}

.dropdown a{
  display:block;
  padding:14px 16px;
  margin-bottom:6px;
  border-radius:14px;
  color:var(--muted);
  text-decoration:none;
  transition:.3s;
}

.dropdown a:hover{
  background:linear-gradient(90deg,var(--primary),var(--accent));
  color:#020617;
  transform:translateX(6px);
}

.menu-item:hover .dropdown{
  opacity:1;
  visibility:visible;
  transform:translateY(0);
}

/* ================= MOBILE MENU ================= */
.hamburger{
  display:none;
  font-size:28px;
  cursor:pointer;
}

/* ================= HERO ================= */
.carousel{
  height:100vh;
  margin-top:80px;
  position:relative;
  overflow:hidden;
}

.slide{
  position:absolute;
  inset:0;
  background-size:cover;
  background-position:center;
  opacity:0;
  transition:opacity 1.2s;
}
.slide.active{opacity:1;}

.overlay{
  position:absolute;
  inset:0;
  background:linear-gradient(90deg,rgba(2,6,23,.9),rgba(2,6,23,.4));
}

.hero-content{
  position:absolute;
  top:50%;
  left:9%;
  transform:translateY(-50%);
  max-width:650px;
}

.hero-content h1{
  font-size:56px;
}
.hero-content span{
  background:linear-gradient(45deg,var(--primary),var(--accent));
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
}
.hero-content p{
  margin:20px 0;
  color:var(--muted);
  font-size:18px;
}

.btn{
  padding:15px 36px;
  border:none;
  border-radius:30px;
  background:linear-gradient(45deg,var(--primary),var(--accent));
  font-size:16px;
  cursor:pointer;
  transition:.4s;
}
.btn:hover{transform:scale(1.08);}

/* ================= SECTIONS ================= */
section{padding:90px 8%;}

.section-title{
  text-align:center;
  margin-bottom:60px;
}
.section-title h2{font-size:40px;}
.section-title p{color:var(--muted);}

.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(260px,1fr));
  gap:30px;
}

.card{
  background:var(--glass);
  padding:35px;
  border-radius:22px;
  box-shadow:0 0 30px rgba(34,211,238,.12);
  transition:.4s;
}
.card:hover{
  transform:translateY(-12px);
  box-shadow:0 0 45px rgba(250,204,21,.25);
}
.card h3{color:var(--primary);margin-bottom:10px;}

/* ================= STATS ================= */
.stats{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(200px,1fr));
  gap:25px;
  text-align:center;
}
.stat{
  background:var(--glass);
  padding:40px;
  border-radius:20px;
}
.stat h3{
  font-size:38px;
  color:var(--accent);
}

/* ================= FOOTER ================= */
footer{
  text-align:center;
  padding:40px;
  color:var(--muted);
}

/* ================= RESPONSIVE ================= */
@media(max-width:900px){
  .menu{
    position:absolute;
    top:70px;
    right:20px;
    background:var(--glass);
    flex-direction:column;
    width:260px;
    border-radius:20px;
    padding:20px;
    display:none;
  }
  .menu.show{display:flex;}
  .hamburger{display:block;}
  .hero-content h1{font-size:38px;}
}
</style>
</head>

<body>

<!-- NAVBAR -->
<nav class="navbar">
  <div class="logo">UltraTrans</div>

  <div class="hamburger" onclick="toggleMenu()">☰</div>

  <div class="menu" id="menu">
    <div class="menu-item"><a href="#">Home</a>
      <div class="dropdown">
        <a href="#">Overview</a>
        <a href="#">Updates</a>
      </div>
    </div>
    <div class="menu-item"><a href="#">Services</a>
      <div class="dropdown">
        <a href="#">Road Freight</a>
        <a href="#">Air Cargo</a>
        <a href="#">Sea Logistics</a>
      </div>
    </div>
    <div class="menu-item"><a href="#">Tracking</a>
      <div class="dropdown">
        <a href="#">Live Tracking</a>
        <a href="#">History</a>
      </div>
    </div>
    <div class="menu-item"><a href="#">Fleet</a>
      <div class="dropdown">
        <a href="#">Vehicles</a>
        <a href="#">Drivers</a>
      </div>
    </div>
  </div>
</nav>

<!-- HERO -->
<section class="carousel">
  <div class="slide active" style="background-image:url('https://images.unsplash.com/photo-1586528116311-ad8dd3c8310d')">
    <div class="overlay"></div>
    <div class="hero-content">
      <h1>Future-Ready <span>Transportation</span></h1>
      <p>AI powered logistics, live fleet tracking and global delivery.</p>
      <button class="btn">Track Shipment</button>
    </div>
  </div>
</section>

<!-- SERVICES -->
<section>
  <div class="section-title">
    <h2>Our Services</h2>
    <p>Smart logistics for modern businesses</p>
  </div>
  <div class="grid">
    <div class="card"><h3>Smart Fleet</h3><p>Real-time GPS & vehicle diagnostics.</p></div>
    <div class="card"><h3>Warehousing</h3><p>Automated storage & inventory control.</p></div>
    <div class="card"><h3>Global Delivery</h3><p>Cross-border logistics made easy.</p></div>
  </div>
</section>

<!-- STATS -->
<section>
  <div class="stats">
    <div class="stat"><h3>50K+</h3><p>Shipments</p></div>
    <div class="stat"><h3>1500+</h3><p>Vehicles</p></div>
    <div class="stat"><h3>99%</h3><p>On-Time</p></div>
    <div class="stat"><h3>60+</h3><p>Countries</p></div>
  </div>
</section>

<footer>
© 2025 UltraTrans • Premium Logistics Platform
</footer>

<script>
let slides=document.querySelectorAll(".slide");
let i=0;
setInterval(()=>{
  slides[i].classList.remove("active");
  i=(i+1)%slides.length;
  slides[i].classList.add("active");
},5000);

function toggleMenu(){
  document.getElementById("menu").classList.toggle("show");
}
</script>

</body>
</html>
