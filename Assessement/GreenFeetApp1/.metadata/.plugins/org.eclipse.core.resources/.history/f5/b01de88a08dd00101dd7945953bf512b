:root{
  --p1:#2563eb;
  --p2:#22c55e;
  --g1:#f0f9ff;
  --g2:#ecfeff;
  --accent:#f97316;
  --card:#ffffffee;
}
*{margin:0;padding:0;box-sizing:border-box;font-family:Poppins,Arial}
body{
  background:linear-gradient(135deg,var(--g1),var(--g2));
  color:#0f172a;overflow-x:hidden;
}

/* ===== NAVBAR ===== */
header{
  position:sticky;top:0;z-index:999;
  background:linear-gradient(135deg,var(--p1),var(--p2));
  padding:14px 20px;
  box-shadow:0 12px 30px rgba(0,0,0,.25);
}
.nav{
  display:flex;justify-content:space-between;align-items:center;
}
.brand{
  color:#fff;font-size:1.3rem;font-weight:700;
  display:flex;gap:10px;align-items:center;
}
.brand i{animation:spin 6s linear infinite}
@keyframes spin{to{transform:rotate(360deg)}}

.menu{
  display:flex;gap:18px;align-items:center;
}
.menu a{
  color:#fff;text-decoration:none;
  padding:10px 14px;border-radius:14px;
  transition:.3s;
}
.menu a:hover{background:rgba(255,255,255,.18)}

.cta{
  background:linear-gradient(135deg,#00e3ae,#00b3ff);
  color:#003;font-weight:600;
}

/* ===== DROPDOWN ===== */
.dropdown{position:relative}
.dropdown > a{
  display:flex;align-items:center;gap:6px;
}
.dropdown i{font-size:.7rem;transition:.3s}

.dropdown-menu{
  position:absolute;top:120%;left:0;
  min-width:200px;
  background:linear-gradient(135deg,var(--p1),var(--p2));
  border-radius:18px;padding:10px;
  box-shadow:0 18px 40px rgba(0,0,0,.35);
  opacity:0;visibility:hidden;
  transform:translateY(10px);
  transition:.35s;
}
.dropdown-menu a{
  display:block;padding:10px 14px;
  border-radius:14px;
}
.dropdown-menu a:hover{
  background:rgba(255,255,255,.18);
  transform:translateX(6px);
}
.dropdown:hover .dropdown-menu{
  opacity:1;visibility:visible;transform:translateY(0);
}
.dropdown:hover i{transform:rotate(180deg)}

.toggle{display:none;color:#fff;font-size:1.4rem}

/* ===== CAROUSEL ===== */
.carousel{
  width:100%;
  height:420px;
  overflow:hidden;
  position:relative;
}

.slides{
  display:flex;
  width:300%;
  height:100%;
  transition:transform 0.8s ease-in-out;
}

.slide{
  width:100%;
  height:100%;
  flex-shrink:0;
  display:flex;
  align-items:center;
  justify-content:center;
}

.slide h2{
  font-size:48px;
  background:rgba(0,0,0,0.5);
  padding:15px 30px;
  border-radius:10px;
}

/* ===== DOTS ===== */
.dots{
  position:absolute;bottom:18px;left:50%;
  transform:translateX(-50%);
  display:flex;gap:10px;
}
.dot{
  width:10px;height:10px;
  border-radius:50%;
  background:rgba(255,255,255,.5);
  cursor:pointer;
}
.dot.active{background:#fff}

/* ===== CARDS ===== */
.cards{
  max-width:1200px;margin:40px auto;
  display:grid;grid-template-columns:repeat(4,1fr);
  gap:18px;padding:0 14px;
}
.card{
  background:var(--card);
  border-radius:22px;padding:22px;
  text-align:center;cursor:pointer;
  transition:.4s;
}
.card:hover{transform:translateY(-10px) scale(1.05)}
.card i{font-size:2.2rem;color:#6a11cb}

/* ===== RESPONSIVE ===== */
@media(max-width:900px){
  .cards{grid-template-columns:repeat(2,1fr)}
  .slide{height:360px}
}
@media(max-width:680px){
  .menu{
    display:none;flex-direction:column;
    position:absolute;right:14px;top:64px;
    background:linear-gradient(135deg,var(--p1),var(--p2));
    padding:14px;border-radius:20px;
  }
  .menu.show{display:flex}
  .toggle{display:block}
  .cards{grid-template-columns:1fr}

  .dropdown-menu{
    position:static;box-shadow:none;
    transform:none;opacity:1;visibility:visible;
    display:none;margin-top:8px;
  }
  .dropdown.active .dropdown-menu{display:block}
}